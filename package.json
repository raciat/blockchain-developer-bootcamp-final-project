{
  "name": "blockchain-developer-bootcamp-final-project",
  "version": "1.0.0",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/raciat/blockchain-developer-bootcamp-final-project"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.3.2",
    "@truffle/hdwallet-provider": "^1.5.1"
  },
  "scripts": {
    "build": "npm install -g truffle && truffle compile && truffle migrate --network ropsten && cd client && npm install && npm run build && npm install -g serve",
    "start": "serve -s client/build/"
  },
  "engines": {
    "node": "12.x"
  }
}
